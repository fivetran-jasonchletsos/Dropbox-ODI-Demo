# Top-level dev environment (NOT for the connector — see connectors/dropbox/requirements.txt)
# Use this for running scripts/inspect_dropbox.py and dbt.
dbt-core>=1.8,<1.10
dbt-snowflake>=1.8,<1.10
requests>=2.31
pandas>=2.2
openpyxl>=3.1
pyarrow>=15.0
